<div class="container">
  <p style="color: green"><%= notice %></p>

  <h1>Articles <%= ENV["TEST"] %></h1>
  

  <div id="articles">
    <% @articles.each do |article| %>
      <%= render article %>
      <p>
        <%= link_to "Show this article", article %>
      </p>
    <% end %>
  </div>

  <%= link_to "New article", new_article_path %>
</div>